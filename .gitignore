__pycache__/
*.pyc
.venv/
venv/
.env
# Everything in data/ is regenerable (STIX bundle, SQLite store, embedding
# cache) — rebuild it with the STIX loader, never commit it.
data/
*.egg-info/
.pytest_cache/
.DS_Store
node_modules/
frontend/dist/
